$kind: http-example
request:
  url: "{{base_url}}/health"
  method: GET
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: |-
      {
        "status": "ok",
        "service": "AutoStream Inflx Agent API",
        "version": "1.0.0"
      }
order: 1000
